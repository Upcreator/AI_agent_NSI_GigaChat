version: '3.8'

services:
  gigachat-service:
    image: gigachat-normalizer:latest 
    ports:
      - "8090:8090"
    environment:
      - GIGACHAT_CREDENTIALS=${GIGACHAT_CREDENTIALS}
    volumes:
      - ./server.log:/home/app/server.log 